
import React from 'react';
import { Calendar, Clock, Users } from 'lucide-react';

const Header = () => {
  return <header className="w-full max-w-5xl animate-fade-in">
      
    </header>;
};

export default Header;
